# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ModularityDemo' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ModularityDemo
  pod 'R.swift'
  pod 'SnapKit', '~> 4.0.0'
  pod 'PromiseKit', '~> 6.8'
  pod 'Moya'
  pod 'Realm'
  pod 'Kingfisher'

  target 'ModularityDemoTests' do
    inherit! :search_paths

  end

end

